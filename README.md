# Pw change while quering

Simple repro to show what happens when you change the password for a user that a driver has authed with.

# Usage

```
# First time only
npm install

# Edit url, username, password in index.js

# Run it
node .
```
